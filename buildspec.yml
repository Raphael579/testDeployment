version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo "Entering the build phase..."
      - mvn package

  post_build:
    commands:
      - echo "Build completed successfully"

artifacts:
  files:
    - "target/test-deployment.jar"
  discard-paths: true